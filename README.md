# Karachi Civic AI — "The City Around You"

> *"Report a problem. We'll find who can fix it."*
> **"آپ مسئلہ بتائیں، ہم ذمہ دار ادارہ تلاش کریں گے۔"**

A production-grade, browser-based civic reporting and automated government routing platform designed specifically for Karachi citizens. Citizens can submit issues using **Photo 📷**, **Voice 🎙️**, or **Text ✍️** (in English, Urdu, or Roman Urdu). The AI understands the report, extracts location, classifies the civic disruption, maps it to the responsible government authority using deterministic database-driven jurisdiction rules, checks for nearby duplicate complaints, prepares formal bilingual complaints, and dispatches official English communications to verified authority contacts.

---

## 🌟 Key Features

1. **Multimodal Citizen Input**:
   - 📷 **Photo**: Supports mobile camera capture (`capture="environment"`), file upload, drag-and-drop, and automated EXIF GPS extraction.
   - 🎙️ **Voice**: Browser MediaRecorder API with live pulse animations, recording timer, audio playback, delete, re-record, and multilingual transcription via Whisper.
   - ✍️ **Text**: Full support for English, Urdu, Roman Urdu, and mixed speech (e.g., *"North Nazimabad Block H mein 4 din se kachra nahi uthaya gaya"*).

2. **Intelligent Location Engine**:
   - Strict priority system: Explicit Map Pin / Manual > Browser GPS (`navigator.geolocation`) > Photo EXIF GPS > Extracted Text > Geocoded Karachi fallback.
   - Interactive OpenStreetMap / Leaflet map with drag-and-drop pin placement and Karachi boundary validation.

3. **Deterministic Authority Routing Engine**:
   - **No Hallucinated Emails**: Official emails are never generated by an LLM; they are strictly retrieved from the database.
   - **KWSB / KWSC** (`info@kwsc.gos.pk`): Clean water supply, pipeline leaks, sewerage network, and sewage overflows.
   - **SSWMB** (`+92 3181030851`, Email: None): Garbage collection and solid waste. Marked as `email_pending` with clear citizen notification: *"An email contact has not been configured for this authority yet."*
   - **KMC** (`mayor@kmc.gos.pk`): Main roads, bridges, storm water drains, safari park, zoo, encroachments, fire & rescue.
   - **Cantonment Boards**:
     - Clifton & DHA (CBC): `info@cbc.gov.pk`
     - Korangi & Landhi (CBKC): `newceo.cbkc@gmail.com`
     - Faisal Cantt (FCB): `faisalcantonmentboard@gmail.com`
     - Malir Cantt (MCB): `pa.cbmalir@gmail.com`
     - Manora Cantt (MNCB): `cbmanora75@gmail.com`
   - **Town Administration** (`mayor@kmc.gos.pk`): Neighborhood streets, potholes, streetlights, and local sanitation.

4. **Multi-Factor Duplicate Detection**:
   - Checks semantic category match + geographic distance (Haversine threshold < 600m) + recent time window.
   - Enables citizens to add support to existing complaints (`report_count` increment) rather than creating redundant tickets.

5. **Bilingual Citizen UX & Official English Dispatch**:
   - Global instant language toggle (**English | اردو**) with zero LLM latency and full `dir="rtl"` layout mirroring.
   - Citizen UI displays both English and Urdu drafts.
   - Outgoing email sent to authorities is **always in formal English**.

6. **Comprehensive Complaint Dashboard**:
   - Live filtering by date (`All`, `Today`, `Last Week`, `Last Month`).
   - Real-time search across categories, locations, reference IDs, and authorities.
   - Full detail view (`/complaints/:id`) with interactive map, original evidence preview, and audit milestone timeline.

---

## 🏛️ System Architecture

```
[ Citizen Browser ]
  ├── Photo (EXIF GPS) / Voice (MediaRecorder) / Text (English | اردو | Roman Urdu)
  └── Leaflet Interactive Map / Browser Geolocation API
       │
       ▼ (HTTP / JSON)
[ FastAPI Backend Engine ]
  ├── 1. Vision Analysis: Google Gemini (visible facts only)
  ├── 2. Audio Transcription: Groq Whisper Large v3
  ├── 3. Text Normalization: Groq Llama / Qwen
  ├── 4. Location Priority Engine: Manual > GPS > EXIF > Text
  ├── 5. Authority Routing: Database-driven rules
  ├── 6. Duplicate Detection: Haversine distance (<600m) + Category + Time
  ├── 7. Dual Complaint Generator: Formal English & Courteous Urdu
  └── 8. Dispatcher: Resend API (idempotent, official English template)
       │
       ▼
[ PostgreSQL / Supabase Schema with pgvector & RLS ]
  ├── authorities & authority_contacts
  ├── authority_jurisdictions (Cantonments & Towns)
  ├── issue_categories (14 categories) & subcategories
  ├── complaints & complaint_embeddings
  └── complaint_events (audit timeline)
```

---

## 📁 Repository Structure

```
├── backend/
│   ├── app/
│   │   ├── api/routes/          # API endpoints (complaints, authorities, location, health)
│   │   ├── core/                # config, security, logging
│   │   ├── repositories/        # complaint_repo, authority_repo
│   │   ├── schemas/             # Pydantic validation schemas
│   │   ├── services/            # gemini_service, groq_service, whisper_service,
│   │   │                        # location_service, routing_service, duplicate_service,
│   │   │                        # email_service, complaint_generator
│   │   ├── utils/               # exif GPS parser, geocoding
│   │   └── main.py              # FastAPI application entrypoint
│   ├── tests/                   # Pytest automated test suite
│   ├── requirements.txt         # Python dependencies
│   ├── .env                     # App configuration & credentials
│   └── .env.example
├── frontend/
│   ├── src/
│   │   ├── components/          # Navbar, LanguageToggle, PhotoUploader, VoiceRecorder,
│   │   │                        # TextReporter, LocationPicker, AnalysisStepper,
│   │   │                        # ComplaintPreview, ComplaintCard, ComplaintTimeline,
│   │   │                        # StatusBadge
│   │   ├── locales/             # en.json, ur.json (100% static translations)
│   │   ├── pages/               # Home, Report, Complaints, ComplaintDetails, Login, Profile
│   │   ├── services/            # api.ts
│   │   ├── types/               # TypeScript interfaces
│   │   ├── i18n.ts              # i18next & RTL direction handler
│   │   ├── index.css            # Tailwind + Leaflet + Google Fonts
│   │   ├── App.tsx              # React router definitions
│   │   └── main.tsx
│   ├── package.json
│   ├── tailwind.config.js       # Pakistan Civic Green color scheme
│   └── vite.config.ts
└── supabase/
    └── migrations/
        └── 20260912000000_init_civic_schema.sql  # Complete schema, RLS, indexes & seed data
```

---

## 🚀 Quickstart & Installation

### Prerequisites
- **Python**: 3.11+
- **Node.js**: 20+
- **npm**: 10+

### 1. Backend Setup
```bash
cd backend

# Create virtual environment (optional)
python -m venv .venv
# On Windows: .venv\Scripts\activate
# On Linux/macOS: source .venv/bin/activate

# Install dependencies
pip install -r requirements.txt

# Start backend server
python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
```
The backend API is accessible at: `http://127.0.0.1:8000`
Interactive API Docs (Swagger): `http://127.0.0.1:8000/docs`
Health check: `http://127.0.0.1:8000/api/health`

### 2. Frontend Setup
```bash
cd frontend

# Install dependencies
npm install

# Build for production
npm run build

# Start development server
npm run dev
```
The application opens at: `http://127.0.0.1:5173`

---

## 🧪 Running Automated Tests

Run the backend test suite:
```bash
cd backend
python -m pytest tests/ -v
```
**Test Coverage Includes:**
- Authority routing rules (KWSB, SSWMB, KMC, CBC, FCB, Town Administration)
- Location priority resolution (Manual > Browser GPS > EXIF GPS > Text)
- Multi-factor duplicate detection (Haversine distance & category match)
- Missing authority email handling (SSWMB returns `email_pending` without inventing emails)
- Health, categories, and end-to-end complaint analysis API endpoints

---

## 🔑 Environment Variables (`backend/.env`)

| Variable | Description | Example |
|---|---|---|
| `SUPABASE_URL` | Supabase project URL | `https://raqcvzaxufmymrfltgrw.supabase.co` |
| `SUPABASE_ANON_KEY` | Public client key | `sb_publishable_...` |
| `SUPABASE_SERVICE_ROLE_KEY` | Administrative service key | `sb_publishable_...` |
| `GROQ_API_KEY` | Groq API key for reasoning and Whisper | `gsk_...` |
| `GEMINI_API_KEY` | Google Gemini API key for visual inspection | `AQ.Ab8RN...` |
| `SMTP_HOST` | Gmail SMTP server host | `smtp.gmail.com` |
| `SMTP_PORT` | Gmail SMTP server port | `587` |
| `SMTP_USERNAME` | Gmail account address | `shahmanjamal9@gmail.com` |
| `SMTP_PASSWORD` | 16-character Google App Password | `foyhkjgpdsuksjgx` |
| `EMAIL_FROM` | Sender email address | `shahmanjamal9@gmail.com` |
| `EMAIL_FROM_NAME` | Sender display name | `cwa_chip` |
| `SMTP_USE_TLS` | Enable TLS encryption | `true` |
| `FRONTEND_URL` | Frontend URL for CORS | `https://cwa-ship-2026.vercel.app` |

---

## 🌐 Full Cloud Deployment Guide

### 1. Deploy Database on Supabase
1. Open your Supabase Dashboard: [https://supabase.com/dashboard](https://supabase.com/dashboard).
2. Go to **SQL Editor** -> **New Query**.
3. Copy the entire contents of [`supabase_complete_schema.sql`](./supabase_complete_schema.sql) and click **Run**.
4. All tables (`authorities`, `complaints`, `complaint_events`, `issue_categories`), RLS policies, and seed data will be created instantly.

### 2. Deploy Backend on Render (Free Web Service)
1. Go to [https://render.com](https://render.com) and click **New +** -> **Web Service**.
2. Connect your GitHub repository: `https://github.com/fahad15fede/CWA-Ship-2026.git`.
3. Configure service settings:
   - **Name**: `cwa-ship-backend`
   - **Root Directory**: `backend`
   - **Runtime**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `uvicorn app.main:app --host 0.0.0.0 --port $PORT`
4. Under **Environment Variables**, add the variables from your `backend/.env`:
   - `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`
   - `GROQ_API_KEY`, `GEMINI_API_KEY`
   - `SMTP_HOST=smtp.gmail.com`, `SMTP_PORT=587`
   - `SMTP_USERNAME=shahmanjamal9@gmail.com`, `SMTP_PASSWORD=foyhkjgpdsuksjgx`
   - `EMAIL_FROM=shahmanjamal9@gmail.com`, `EMAIL_FROM_NAME=cwa_chip`, `SMTP_USE_TLS=true`
5. Click **Create Web Service**. Once deployed, Render will provide your public backend URL, e.g. `https://cwa-ship-backend.onrender.com`.

### 3. Deploy Frontend on Vercel
1. Go to [https://vercel.com](https://vercel.com) and click **Add New...** -> **Project**.
2. Import your GitHub repository: `https://github.com/fahad15fede/CWA-Ship-2026.git`.
3. In project settings:
   - **Framework Preset**: `Vite`
   - **Root Directory**: Click "Edit" and select `frontend` (or leave as root; `vercel.json` will build automatically)
4. Under **Environment Variables**, add:
   - `VITE_API_BASE_URL` = `https://cwa-ship-backend.onrender.com` (Your Render backend URL)
5. Click **Deploy**. Vercel will build and assign your live production URL (e.g. `https://cwa-ship-2026.vercel.app`)!

## ⏱️ 60-Second Hero Demo Flow

1. Open `http://localhost:5173`.
2. Observe the civic visual identity and 3 input mode cards (**Photo 📷**, **Voice 🎙️**, **Text ✍️**).
3. Click the **اردو** language toggle in the navbar. Notice the entire interface seamlessly flips to RTL with native Urdu translations and typography. Switch back to **English**.
4. Click **"Report an Issue"**.
5. Select the **Text** tab and paste:
   > *"North Nazimabad Block H mein 4 din se kachra nahi uthaya gaya"*
6. Notice the location is detected as **North Nazimabad, Karachi**. Click **"Analyze & Route Issue"**.
7. Watch the animated 6-stage analysis stepper complete.
8. View the result card:
   - Category: **Garbage & Solid Waste**
   - Responsible Authority: **Sindh Solid Waste Management Board (SSWMB)**
   - Notice: *"An email contact has not been configured for this authority yet."* (with phone helpline: `+92 3181030851`).
   - Bilingual preview tabs (**English** for official email, **اردو** for citizen review).
9. Next, submit a sewage issue: *"Severe sewage overflow on Shahrah-e-Faisal near Nursery"*.
10. Notice it is accurately routed to **Karachi Water & Sewerage Corporation (KWSB)** (`info@kwsc.gos.pk`).
11. Click **"Send Complaint"** — the official English complaint is dispatched, returning reference ID `CWA-YYYYMMDD-XXX`.
12. Click **"View My Complaints"** — browse the dashboard with filters (**All**, **Today**, **Last Week**, **Last Month**), search, and view detailed timelines.
